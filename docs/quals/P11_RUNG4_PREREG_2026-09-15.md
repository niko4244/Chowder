# P11 rung 4 — longer-horizon paired CUDA qualification

Frozen **2026-09-15, before the run**. Committed to `niko4244/Chowder` and
pushed **before** `chowder train` is invoked. This rung extends rung 3c from
12-step trainability to 48-step routing effectiveness on the same 9B-derived
HotCore artifact, adding an independent holdout and a generation sanity probe.

## Governing documents

- Rung-3c preregistration: `docs/quals/P11_RUNG3C_CUDA_PAIRED_PREREG_2026-09-15.md`
  (PR #162, squash-merged as `809f4da`).
- Rung-3c result: `Chowder-Protected/runs/2026-09-15-router-healing-rung3c-paired-cuda/P11_RUNG3C_CUDA_PAIRED_RESULT_2026-09-15.md`
  — QUALIFIED with one recorded wall-charge exceedance (later diagnosed as
  ledger double-charge, fixed in PR #164).
- Template: `docs/quals/PREREG_TEMPLATE.md`.

## 1. Identity

| Field | Value |
|---|---|
| Run name / ladder rung | P11 rung 4 — longer-horizon paired CUDA |
| Base artifact | `F:\llm-models\Qwen3.8-9B-HotCore-CW-E16-k2-h2176` |
| Content manifest pin | SHA-256 `77520edadb9a94f4ed70636328c4bbbaafa49c75e3b47c51418851c5ad4869c4` |
| Source parent | `F:\llm-models\Qwen3.8-9B-abliterated-25-bf16` (verified lineage via tokenizer SHA, provenance `source_dir`, conversion manifest) |
| Device + card memory | CUDA device 0, RTX 5060 Ti, 17.1 GB |
| Branch/PR carrying this prereg | To be committed on `main` before execution |

## 2. Workload (frozen)

| Field | Value |
|---|---|
| Model | HotCore E16/k2/h2176 (9B-derived MoE) |
| Steps / horizon | 48 steps, `max_steps` stop expected |
| tokens/step | 1536 (seq_len=64, batch_size=2) |
| Training corpus | `router-corpus-9b-pilot.txt` (512 deterministic sentences), SHA-256 `15d5f5f51a739ceee2712fe5b7b550982aba7272f633781f06d5a7ea64f47941` |
| Independent holdout corpus | `router-holdout-independent-9b.txt` — **NEW**, distinct from training corpus and from channel-ranking calibration corpus, byte-pinned separately |
| Policy / dtype | `bf16-offload-transient`, `torch.bfloat16` |
| paired_arms | `true` |
| learning_rate / seed / probe_window | 0.05 / 1 / 2 |
| eval_batches | 2 |

### Holdout corpus specification

The independent holdout must satisfy:
- **Disjoint** from training corpus (`router-corpus-9b-pilot.txt`)
- **Disjoint** from channel-ranking calibration corpus (SHA `4cdf55d544b2abcff6f3d4d2a2e276be5314698af8783e998c2fa296e7fe99dc`)
- **Disjoint** from the protected parent tournament suite
- Minimum 64 sentences, deterministic, byte-pinned
- SHA-256 recorded in this prereg before execution

**Holdout SHA-256**: `2e99668207319a1d2b702408bcc659a525fd226d027eebeaebea918e2ad21e97`

## 3. Budgets (all enforced by preflight)

### Measured basis (from rung 3c)

| Component | Rung-3c measured |
|---|---|
| Step cost | 2.854 s/step (successful run); 3.034 s/step (refused probe, within jitter) |
| Model load (train) | 13.851 s = 0.0038475 GPU-h |
| Model load (eval) | 13.609 s = 0.0037802 GPU-h |
| Device total | 0.0166941 GPU-h ≈ 60.1 s |
| Wall multiplier M | 3.5 (0.0585 wall / 0.0166941 device) |
| Peak memory (train) | 13.503 GB |
| Peak memory (eval) | 9.482 GB |

### Rung-4 projection (48 steps, same class)

| Component | Projection |
|---|---|
| Step cost (48 steps) | 48 × 2.854 s = 137.0 s |
| Model loads (2: train + eval) | 13.851 + 13.609 = 27.5 s |
| Generation + publication | ~7.5 s (estimated, proportional to rung 3c) |
| **Device total** | **172.0 s ≈ 0.0478 GPU-h** |
| **×1.5 margin → device ceiling** | **0.0717 GPU-h ≈ 0.072 GPU-h** |
| Wall envelope (M=3.5) | 0.072 × 3.5 = **0.252 wall GPU-h** |

### Enforced ceilings

| Budget | Ceiling | Decomposition |
|---|---|---|
| Aggregate device | **0.072 GPU-h** | loads 0.0114 / steps 0.0574 / generations 0.0032 |
| `max_load_seconds` | **20.0 s** per load | (13.851 measured × 1.5) |
| Step cost line | **3.5 s/step** | (3.034 worst probe × 1.15 margin) |
| Memory refusal | **14.5 GB** peak | (13.503 measured + 1.0 GB margin) |
| Wall envelope | **0.252 GPU-h** | (device ceiling × M = 3.5) |

## 4. Thresholds (fixed before the run)

### T1 — validate before train
`chowder project-validate` passes; `chowder train` invoked only after.

### T2 — policy contract
Every worker result carries `load_policy_report.policy = "bf16-offload-transient"`,
`dtype = torch.bfloat16`, `placement_census` with all expert params on CPU,
gates on `cuda:0`, `verified: true`.

### T3 — measured preflight
Memory, step-cost, and model-load projections measured before step 1.
Refusal if: load + steps + evaluation projection exceeds 0.072 ceiling,
3.5 s/step, or 14.5 GB.

### T4 — trainability
Finite non-zero gate gradients on the exact gate path, real update,
32 router gates in scope; frozen-tensor digests report zero changed.

### T4a — routing-collapse threshold

| Metric | Threshold |
|---|---|
| `dead_experts_after` | **< 428** (strict progress from rung 3c's 428/512) |
| `layers_with_grad_zero` | **≤ 2 of 32** (tightened from rung 3c's 3/32) |
| Any layer with `grad_zero_steps == 48` | Must be named with topology-vs-optimizer evidence |
| Missing per-layer records | **UNKNOWN** — refuses to certify |

**Baseline reference**: rung 3c measured 428/512 dead experts after 12 steps.
Rung 4 requires strict de-collapse progress: fewer dead experts, not equal.

### T5 — horizon
48/48 steps, `stop_reason = max_steps`.

### T6 — paired gate contract
Exactly one evaluation worker for the candidate; `arm = "paired"`; base
measured **before** any payload read; single measured `model_load`; both
generations measured; baseline row completed from paired evidence.

### T7 — accounting
Total attributable device GPU-hours ≤ **0.072**, with loads explicitly budgeted.
Wall charges within the 0.252 envelope or exceedance honestly recorded.

### T8 — identity chain
Full-mode base content identity matches pinned manifest hash; payload bound
to re-derived base content; corpora hash to their pins; spec digest and
worker source identity verified.

### T9 — independent holdout quality (NEW)

**Baseline re-measurement**: the frozen baseline (no candidate applied) must
be scored on the independent holdout corpus before the candidate training
run. This establishes the baseline holdout loss independently of the training
protocol.

**Threshold**: `candidate_holdout_loss < baseline_holdout_loss`

This is a directional criterion, not a margin requirement. The candidate
must improve on a genuinely independent holdout. A regression or equality
is a FAIL. The threshold is the direction of improvement, not a specific
delta — the margin is left to the evidence because this is the first
independent holdout measurement.

**Rationale**: rung 3c scored only the training protocol corpus. The static-prune
experiment (finding #4) showed that loss/perplexity can appear acceptable while
generation collapses. An independent holdout is the minimum additional signal
before longer-horizon claims.

### T10 — generation sanity probe (NEW)

After the normal holdout evaluation, run a small generation sanity check
on both base and candidate using the same prompts and settings.

**Required metrics** (all measured, not derived from loss):

1. **Termination rate**: fraction of prompts that terminate with `eos` within
   the max-token limit (target: ≥ 0.90)
2. **Max-token-cap hit rate**: fraction of prompts hitting the generation limit
   without terminating (target: < 0.10)
3. **Distinct-trigram ratio**: unique trigrams / total trigrams across all
   completions (target: > 0.70 — indicates no severe repetition)
4. **Compression ratio**: output length / input length (reported, not gated —
   diagnostic only)
5. **Obvious looping**: any prompt producing 3+ consecutive identical trigrams
   = FAIL on that prompt (target: 0 looping prompts)
6. **Task score** (where applicable): any prompt with a verifiable answer
   scored correct/incorrect (reported, not gated — diagnostic only)

**Threshold**: `generation_sanity_pass = true` — all of (1), (2), (3), (5) must
meet their targets. (4) and (6) are reported for evidence but do not gate.

**Settings**: identical for base and candidate. Same temperature, top_p,
max_tokens, system prompt. Recorded in the result doc.

## 5. Interpretation rules

- Judge exit 0 only when every threshold is PASS; UNKNOWN refuses to certify.
- Wall-charge exceedance of the envelope: recorded in the result doc by a
  human, never auto-failed (device basis governs refusal).
- All attempts preserved: budget-refused attempts are evidence, and any
  surviving run must pass every threshold.
- No threshold is renegotiated after seeing the data.

## 6. Judge

Judge script: `docs/quals/judge_rung4_2026-09-15.py` (to be committed on the
same PR, dry-run against rung-3c artifacts before the run — must refuse
rung-3c for the new T4a/T9/T10 thresholds it adds).
