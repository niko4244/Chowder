# Generation 1 — protocol-compliance cycle result (2026-09-17)

Judged against `docs/quals/GEN1_PREREG_2026-09-17.md` (frozen before any
compute) plus Amendments 1–3, each written from measurement before the next
compute step. Verdict produced by the mechanical judge (`run_gen1_cycle.py
judge`) over durable evidence; preserved verbatim below.

## Verdict

**PROMOTED.** The frozen promotion rule adjudicated a real Model N → N+1
cycle on the Qwen3.8 9B lineage, end to end, through production
infrastructure. `generation gen1` is recorded in the append-only
`GenerationLedger` beside `gen0`.

## The measured claim

Target: chat-protocol compliance (terminate turns / close thinking), chosen
from the Gen-0 freeze's diagnostics evidence (EOS 0.000, cap-hit 1.000,
unclosed-think 12/16, role-marker imitation). Instrument:
`generation-diagnostics@gen1-eval-protocol-v1` — the 16 frozen prompts under
the protocol-identical Gen-0 contract (greedy, seed 1234, chat template).

| Metric | Gen-0 (frozen) | Gen-1 candidate (measured) | Gate |
| --- | --- | --- | --- |
| EOS termination rate | 0.000 | **1.000** | target Δ ≥ 0.90 ✅ (Δ = +1.000) |
| Max-token-cap hit rate | 1.000 | **0.000** | secondary (implied) ✅ |
| Unclosed-`<think>` rate | 0.750 | **0.000** | ≤ 0.250 ✅ |
| Obvious looping | — | **0 prompts** | = 0 ✅ |
| Distinct-trigram ratio (mean) | — | **0.9732** | ≥ 0.900 ✅ |
| Correctness (16-diag exact match) | 1/16 | 3/16 | improvement, not gated |

Mechanical verdict (`judge_outcome.json`):

| Check | Result |
| --- | --- |
| target:generation-diagnostics@gen1-eval-protocol-v1 | **improved** |
| target_improvement | **met** |
| protected:math500@2024-04 | not-regressed (aggregate-only, Δ 0.000) |
| protected:mgsm@2022-11 | **ok** (48 paired samples, statistically flat) |
| protected_regression | **ok** |
| broad_battery | **ok** (carried rows adjudicated) |
| calibration | unmeasured (declared empty — no affordable instrument) |
| reliability | unmeasured (declared empty — no pass@k eval fits budget) |
| resource_envelope | **ok** (0.829 wall GPU-h ≤ 1.00 eval ceiling) |
| evidence_integrity | **ok** (firewall CLEAN on all three benchmarks) |
| **Verdict** | **PROMOTED** — all predeclared promotion checks passed |

## Execution provenance

- Parent: Gen-0 freeze (`2026-09-16-gen0-eval-freeze`), dense
  `F:\llm-models\Qwen3.8-9B-abliterated-25-bf16`, content-bound manifest,
  ledger version `gen0`.
- Training: production binding (`SubprocessTrainingFn`) → real
  `project-validate` + real `chowder train` (attempt-10 = recipe-a, 30/30
  steps, final train loss 1.655; attempt-11 = recipe-b, 30/30, also
  succeeded). Both adapters durable with per-file SHA-256
  (recipe-a: `ca8769c5…`). WDDM first-GPU-process rule held: zero native
  crashes in the amended configuration.
- Training cost (wall-charged): recipe-a **0.4325 GPU-h**, recipe-b
  **0.3969 GPU-h** — within the amended 0.30-project/0.75-wall envelopes
  charged in the engine's actual units (Amendment 3 C2).
- Candidate evaluation: independent evaluator process, adapter applied over
  the same base, offload placement (`params on cuda=0 cpu=683`), 0.260
  GPU-h.
- Contamination: real fingerprints (16 instrument prompts + 524 real
  math500/mgsm test items) registered before admission; curriculum checked
  CLEAN pre-compute; candidate manifest CLEAN on all evaluated benchmarks.
- Registry: attempt rows terminal (`passed`/`rejected`), no stranded
  results. Recipe-b's row records `rejected` (promoted experiment is
  recipe-a by frozen order).

## Defects found and fixed during adjudication (with regression tests)

1. `compare()` zero-variance branch returned `inconclusive` for any delta,
   making a hard 0→1 target lift unpromotable. Fixed: direction decided by
   `min_effect` symmetrically (test:
   `test_degenerate_parent_floor_target_improvement_is_recognized`).
2. Aggregate-only protected rows were permanently `inconclusive`. Fixed:
   within-tolerance non-drops certify as `not-regressed`; drops beyond
   tolerance still violate (93 binding/decision tests green).
3. Driver manifest wiring supplied no per-benchmark samples → blank
   "not checked" rows; fixed to real CLEAN verdicts.
4. Ledger's first gen1 write carried a stale pre-amendment recipe block;
   corrected with `superseded_values` preserving the original (append-only
   history intact — the rerun correctly refused a duplicate write).

## What PROMOTED does and does not claim

It claims: the autonomous loop executed for real on the 9B lineage and the
frozen rule adjudicated the candidate a legitimate generation — protocol
compliance purchased at no measured regression on any protected row.

It does NOT claim: general capability improvement. math500/mgsm remain
0.0 at the parent's floor (carried), calibration/reliability are honestly
unmeasured, and a 30-step curriculum cannot teach arithmetic. The next
cycle's target must come from fresh measured evidence, per the same
discipline this cycle followed.
